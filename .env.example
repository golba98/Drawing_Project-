# No environment variables are required for the current static app.
# The app runs in the browser and persists data in localStorage.
#
# Add safe placeholder values here if future features require configuration.
